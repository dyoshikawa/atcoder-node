{
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf */*.js",
    "test": "jest",
    "start": "yarn build && node"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "jest": "^24.8.0",
    "prettier": "^1.17.1",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.4.5"
  }
}
