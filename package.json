{
  "scripts": {
    "build": "tsc",
    "test": "yarn build && jest",
    "start": "yarn build && node"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "jest": "^24.8.0",
    "prettier": "^1.17.1",
    "typescript": "^3.4.5"
  }
}
